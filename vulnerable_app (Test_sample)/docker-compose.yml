version: "3.8"

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      # SECURITY ISSUE: Hardcoded secrets in compose file
      SECRET_KEY: dev_secret_key_12345_change_in_production
      JWT_SECRET: jwt_super_secret_do_not_share
      AWS_ACCESS_KEY_ID: AKIAIOSFODNN7EXAMPLE
      AWS_SECRET_ACCESS_KEY: wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
      DATABASE_URL: sqlite:///securevault.db
      DEBUG: "True"
    volumes:
      # SECURITY ISSUE: Overly broad mount
      - /:/host:ro
